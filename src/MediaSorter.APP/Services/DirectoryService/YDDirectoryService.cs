using MediaSorter.Core.Entities;
using MediaSorter.Core.Services;

namespace MediaSorter.APP.Services;

public class YDDirectoryService : BaseDirectoryService
{
    public override Folder AnalyzeDirectory(string path)
    {
        return base.AnalyzeDirectory(path);
    }
}